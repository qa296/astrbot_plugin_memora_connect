"""Legacy group isolation verification script for AstrBot Memora Connect.

The original implementation was removed during refactoring, but CI still
expects this entry point to exist. This stub keeps compatibility without
adding extra runtime behaviour.
"""

if __name__ == "__main__":  # pragma: no cover
    print("verify_group_isolation: no-op stub; group isolation is covered in core logic.")
